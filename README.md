Build the JSON file dynamically using the make_manifest.json.
Store your Google Oauth Token in a local .env file as follows:
GOOGLE_OAUTH_TOKEN=YOUROAUTHTOKEN
To build the manifest.json run the following commands:
    npm install
    npm run build

